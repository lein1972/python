// index.js
/**
 * Funcion simple para sumar dos numeros.
 * Creada para practicar la integracion de Git con VS Code.
 */
function sumar(a, b) {
    return a + b;
}

const resultado = sumar(15, 7);
console.log("El resultado de la suma es:", resultado);